# 2026-05-19 工作日志

## 主要工作：算法化注入修复 + 自测比对

### 修复内容
1. **structuredC.js 从空文件恢复** — 上次会话文件被误清空，从 git 恢复（2555行）
2. **三个提取函数迁移** — 在 structuredC.js 第1965行后插入：
   - `extractStableFramesFromContent()` — 提取稳定帧
   - `extractPropStateChangesFromContent()` — 提取道具状态变化
   - `extractSemanticLocationRulesFromContent()` — 提取语义位置规则（排除道具内部）
3. **enrichSegmentShots 签名更新** — 新增 `directorNotes`, `annotation`, `segIndex`, `totalSegs` 参数
4. **lines变量作用域修复** — 原代码每个 if 块各自 `const lines = userPrompt.split('\n')`，导致注入不累积；改为函数作用域 `let lines = userPrompt.split('\n')` 统一操作
5. **pipeline.js const→let** — `const renderedSegments = []` → `let renderedSegments = []`，修复 `Assignment to constant variable` 运行时报错
6. **deduplicateARenderings** — 已在 pipeline.js 中正确实现

### 自测结果（DeepSeek v4-flash API）
- 登录：✅ admin/admin123
- AI调用：✅ deepseek-v4-flash 成功，每片段约20秒
- 道具状态注入 enrich log 确认：`[注入] 道具状态 49字` + `已插入prompt`
- 输出2954字，包含正确的场景分隔符和【A】画面物理系统
- 全流程无报错，status=done

### 关键API信息
- 用户API Key对应服务商：DeepSeek（非OpenAI）
- 可用模型：deepseek-v4-flash, deepseek-v4-pro
- baseUrl：https://api.deepseek.com

### 文件状态
- `lib/structuredC.js` — 已修复，通过 node -c
- `lib/pipeline.js` — 已修复，通过 node -c
- 服务端口：3006（已重启）

## P1修复完成：buildBatchEnrichPrompt 导演讲戏注入（2026-05-19）

**问题**：buildBatchEnrichPrompt 缺少 directorNotes 参数，且物理变量使用了 derivePhysics 不存在的字段名（camera/surface/soundDesign）。

**修复内容**（structuredC.js）：
1. 函数签名增加 `directorNotes` 参数
2. 物理变量改用真实字段：`directorPhysics.light/material`（来自 derivePhysics）+ atmosphereOverride 注入
3. 【场景物理系统】区块末尾追加"导演氛围指示"行
4. 调用处 line 2683 透传 directorNotes

**当前分支**：0512，服务端口 3006（PID 23852）

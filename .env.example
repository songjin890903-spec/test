# 视频提示词工具 - 环境变量说明
# 本工具的 API Key 由用户在前端页面填写，通过 HTTPS 请求传入后端，
# 无需在服务端配置 API Key 环境变量。
#
# Railway 部署时只需配置以下可选变量：

# 服务端口（Railway 会自动注入 PORT，通常不需要手动填写）
# PORT=3001

# （可选）如果你想在服务端固定一个默认 API Key，可在此填写
# DEFAULT_API_KEY=sk-xxxxxxxx
